import { BrowserRouter, Routes, Route } from "react-router-dom";

const router = () => {
    return (
        <BrowserRouter>
            <Routes>

            </Routes>
        </BrowserRouter>
    )
}

export default router; 